"""Shared pytest fixtures.

Pytest auto-discovers this file for every test under ``tests/``. Add fixtures
that more than one test needs (DB sessions, fake clocks, tmp dirs with a
seeded layout). One-off helpers belong in the test file that uses them.
"""

from __future__ import annotations

# Nothing shared yet — add fixtures as the suite grows.
